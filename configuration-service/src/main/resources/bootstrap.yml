spring:
  application:
    name: configuration-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/drm3107/cloud-native-reference-configuration
server:
  port: 8888
