spring:
  application:
    name: configuration-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/drm317/cloud-native-reference-configuration
server:
  port: 8888