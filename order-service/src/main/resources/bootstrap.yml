server:
  port: 8900
spring:
  application:
    name: order-service
  cloud:
    config:
      uri: http://configuration-service:8888
      retry:
        initialInterval: 2000
        maxAttempts: 30